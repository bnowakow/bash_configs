
# TODO replace below with one that allows only
# /usr/bin/su sup -c 'cd /mnt/MargokPool/home/sup/code/nordvpn-torrent; vagrant ssh -c "if service transmission-daemon status | grep Active | grep running> /dev/null; then echo true; else echo false; fi" 2>/dev/null'
Cmnd_Alias ZABBIX_CMD=/bin/helm ls --all-namespaces --kubeconfig /etc/rancher/k3s/k3s.yaml,/usr/bin/su
zabbix ALL=NOPASSWD: ZABBIX_CMD
