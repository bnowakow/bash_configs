persistence:
  config:
    enabled: true
    path: /mnt/MargokPool/archive/Data/rancher/prowlarr
    server: nas.localdomain.bnowakowski.pl
    type: nfs
    targetSelector:
      exportarr:
        exportarr:
          mountPath: /config
          readOnly: true
      main:
        main:
          mountPath: /config
