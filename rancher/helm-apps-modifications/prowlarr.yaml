persistence:
  config:
    enabled: true
    path: /mnt/MargokPool/archive/Data/rancher/prowlarr
    server: 192.168.0.20
    type: nfs
    targetSelector:
      exportarr:
        exportarr:
          mountPath: /config
          readOnly: true
      main:
        main:
          mountPath: /config
