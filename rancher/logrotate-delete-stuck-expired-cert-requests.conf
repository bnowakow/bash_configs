# Logrotate config for delete-stuck-expired-cert-requests cron logs
/var/log/delete-stuck-expired-cert-requests.log {
    daily
    rotate 30
    maxage 90
    missingok
    notifempty
    compress
    delaycompress
    create 0644 root root
}
