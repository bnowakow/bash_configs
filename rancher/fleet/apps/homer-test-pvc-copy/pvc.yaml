apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homer-test-config-longhorn
  namespace: apps-homer-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1273Ki
