# Copy to /etc/traefik/traefik.env and replace the placeholder.
# Create a scoped Cloudflare token: Zone:Read and DNS:Edit only, for the two zones.
CF_DNS_API_TOKEN_FILE=/etc/traefik/credentials/cloudflare-dns-api-token
